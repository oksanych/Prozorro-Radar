# Prozorro Radar

Use the Context7 MCP server for up-to-date, version-specific documentation when working with framework or library behavior in this project, especially Next.js App Router, React server/client components, NextAuth, Tailwind CSS, Vitest, TypeScript, and better-sqlite3.

Prefer local project guidance in `CLAUDE.md` for project-specific architecture, commands, data pipeline rules, scoring terminology, and Prozorro API pitfalls.
